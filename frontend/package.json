{
  "name": "raveenthiran-frontend",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Headless Astro frontend for raveenthiran.com — pulls the Work archive from WordPress (wp.m1o.at) at build time and outputs a static site for easyname.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.13.0"
  }
}
